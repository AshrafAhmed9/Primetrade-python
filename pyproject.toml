[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100

[tool.black]
line-length = 100

[project.optional-dependencies]
dev = ["pytest==8.1.0", "pytest-mock==3.14.0", "black==24.4.0", "ruff==0.4.0"]
